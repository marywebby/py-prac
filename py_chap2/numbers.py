# integers 
    # add and subtract
3 + 4 
4 - 3 
8 * 10
15 / 3
    # exponents 
4 ** 3
3 ** 3 
10 ** 3 
    # pemdas 
2 + 3*4 
(2 + 3) * 4 

# floats 
    # python calls any number with a deicaml point a float 
0.1 + 0.1 
0.2 + 0.2 
2 * 0.1 
2 * 0.2 
    # when you divide any two numbers. even if hey are integers that result in a whole number, youll always get a float
4/2 
    # if you mix an integer and a float in any other operation, youll get a float too
1 + 2.0
2 * 3.0
3.0 ** 2
    # python defaults to a float in any operation that uses a float, even if the output is a whole number 

# underscores in numbers 
    # when writing long #'s, group digits using underscores to make it more readable. 
universe_age = 14_000_000_000
print(universe_age)
    
# multiple assignment 
    # shorter programs of code by assaining more than onve varibale using just one line of code 
    # used most often when initializing a set of numbers 
x, y, z = 0, 1, 4
print(x)
print(y)
print(z)

# constants 
    # constant is a varibale whose value stays the same throughout the life of a program 
    # python does not have any built in constant types
    # programmers will use all capital letters to indicate a varibale should be treated as a constant and never changed 
MAX_CONNECTIONS = 5000




